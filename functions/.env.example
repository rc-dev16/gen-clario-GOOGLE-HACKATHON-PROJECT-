# Cloud Functions config (deployed). Copy to .env — never commit real secrets.
# Do not put local filesystem paths to service-account JSON here.
DOCUMENT_AI_PROCESSOR_ID=
DOCUMENT_AI_LOCATION=us
CLARIO_UPLOAD_BUCKET=
GEMINI_LOCATION=us-central1
GEMINI_MODEL=gemini-2.5-flash-lite
